<?php

if (!$this->item['disabled']) {
    echo '<a class="remove glyphicon glyphicon-trash" href="' . route(\null, ['action' => 'delete', 'id' => $this->item['id']]) . '"> </a>';
} else {
    echo ' - ';
}